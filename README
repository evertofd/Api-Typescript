## Pre-requisitos clipboard
Preinstalar:

- Node Js
- Typescript
- PostgreSql

## Instalación
Una vez descargado, antes de iniciar, instalar dependencias con "npm install" en consola de Node

npm install
Para desarrollo "npm run dev"
Para producción "npm run build" y luego "node dist/index.js"

Crear base de datos en Postgresql:

- CREATE DATABASE api_typescript

Crear tabla user con:

- CREATE TABLE users(id PRIMARY KEY, name VARCHAR(40), email UNIQUE);

Cambiar credenciales de conexión.


## Construido con:
- Node js, Typescript, PostgreSql y mucho amor!

## Autores
Everto Farias
